services:
  frontend:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      BASE_BACKEND_URL: https://jsonplaceholder.typicode.com
#  backend:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    ports:
#      - "4000:8080"